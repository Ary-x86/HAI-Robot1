# --- OpenAI ---
OPENAI_API_KEY=sk-proj-

# --- Robot / WAMP ---
RIDK_REALM=rie.6942eb0ec5f92c2184a60e9a
RIDK_WAMP_URL=ws://wamp.robotsindeklas.nl

# --- STT Provider ---
STT_PROVIDER=openai
USE_OPENAI_STT=0
STT_MODEL=gpt-4o-mini-transcribe
STT_FALLBACK_TO_GOOGLE=1

# --- STT QUALITY GATES (CRITICAL FIX) ---
# Drop ultra-short noise bursts (clicks, breaths, robot audio bleed)
STT_MIN_AUDIO_SEC=0.25

# Drop quiet background hiss that OpenAI turns into random words
STT_MIN_RMS=220

# Force English to avoid "Dios / Taip / 好" hallucinations
STT_OPENAI_LANGUAGE=en

# Ignore 1-word utterances during gameplay (YES/NO still works on rematch)
IGNORE_ONE_WORD_MIDGAME=1

# Debug audio stats so you can tune thresholds live if needed
STT_DEBUG_AUDIO=0